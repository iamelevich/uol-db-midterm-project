<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%- settings.blog_title %>: <%- title %></title>
    <link rel="stylesheet" href="/dist.css" />

    <!-- EVA Icons -->
    <script src="https://unpkg.com/eva-icons"></script>

    <!-- Alpine Plugins -->
    <script defer src="https://unpkg.com/@alpinejs/persist@3.x.x/dist/cdn.min.js"></script>
    <!-- Application JS -->
    <script defer src="/app.js"></script>
    <!-- Alpine Core -->
    <script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"></script>
  </head>
  <body x-data :class="$store.darkMode.on && 'dark bg-slate-700'">
    <div class="container mx-auto dark:text-white dark:fill-white min-h-screen flex flex-col">
      <header class="flex-none py-4 flex">
        <a class="flex-none text-2xl uppercase gradient-link" href="/"><%- settings.blog_subtitle %></a>
        <span class="flex-none pl-1 text-sm text-slate-500 dark:text-slate-300"
          >by <%- settings.blog_author %></span
        >
        <nav class="grow flex justify-end items-center">
          <a class="inline-block align-middle gradient-link px-2" href="/admin">Admin</a>
          <a class="inline-block align-middle gradient-link px-2" href="/admin/settings">Settings</a>
        </nav>
        <button x-data @click="$store.darkMode.toggle()" class="flex-none">
          <i x-show="!$store.darkMode.on" data-eva="moon-outline"></i>
          <i x-show="$store.darkMode.on" data-eva="sun-outline"></i>
        </button>
      </header>
      <main class="grow"><%- body %></main>
      <footer class="flex-none py-4 text-right">by <%- settings.blog_author %></footer>
    </div>
  </body>
</html>
